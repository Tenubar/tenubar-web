# My Portfolio! Tenubar

## Social icons

Social icon names in `src/config.ts` must match the identifiers provided by `astro-icon` (see `.astro/icon.d.ts`). Invalid icon names will cause the Astro build to fail.
